import { createStore } from 'vuex'
import lifeListStore from "../modules/lifeList/store";

export default createStore({
  state: {
  },
  mutations: {
  },
  actions: {
  },
  getters: {
  },
  modules: {
    lifeListStore,
  }
})
